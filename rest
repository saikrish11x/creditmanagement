<?php
$servername = "localhost";
$username = "root";
$password = "";
$dbname = "credituser";
$conn = new mysqli($servername, $username, $password, $dbname);
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);   
}
$name = $_GET['name'];
$query="select * from userdetails where name='" . $name . "'";
$result=$conn->query($query); 
$row=mysqli_fetch_assoc($result);
$query = "select name from users where name<>'" . $row['name'] . "'";
$result=$conn->query($query);   
?>
<html>
<head>
<title>transfer credits</title>
</head>
<body>
    <a href="user.php">&lt Back</a>
    <?php
    echo "<h1> Welcome " .$row['name']."</h1>";  
    echo "<h4> Your current credits are " .$row['currentcredit']."<h4>";
    ?>
    <h2>Tranfer credits</h2>
    <form action="" method="post">
        credits <input type="number" name="credit_trans" min=0 max=100>  
        <br><br>   
        Tranfer To:  
        <select name="to_user " >
        <option value =""></option>
        <?php
        while($tname = mysqli_fetch_array($result)){        
        echo "<option value='" . $tname['name'] . "'>" . $tname['name'] . "</option>";
        }
        ?>
        </select>
    </form>
</body>
</html>